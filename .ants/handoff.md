NEXT: atlas
REASON: El Director ha redefinido la dirección del producto hacia un monorepo Nx con un backend NestJS + TypeORM + SQLite. Esta es una decisión de arquitectura y estructura de archivos que requiere planificación técnica detallada, migración ordenada del frontend existente y setup inicial del backend. Atlas es la hormiga especializada en arquitectura, estructura y patrones de diseño.
TASK: Crear el plan de migración a monorepo Nx, inicializar el workspace, mover el frontend React + Vite como una app de Nx (`apps/web`) sin romper funcionalidad, y crear el esqueleto del backend NestJS (`apps/api`) con TypeORM + SQLite. Dejar el frontend compilando, tests pasando y el backend arrancando con una entidad base de ejemplo (p. ej. `App` o `Category`). Actualizar AGENTS.md con la estructura final y dejar handoff para kael/echo según lo que surja.
CONTEXT: El marketplace es una SPA React + Vite + TypeScript + TailwindCSS v4 con datos estáticos en `src/data/apps.ts`, favoritos/recientes en `localStorage`, tests con Vitest y E2E con Playwright, CI/CD en GitHub Actions y Docker con nginx. No existe backend ni base de datos. El PR #13 de echo aún está en draft mejorando CI/CD; la migración a Nx probablemente requiera ajustes en pipelines y Docker, por lo que atlas debe coordinar con echo después del setup inicial.
