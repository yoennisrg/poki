NEXT: piper
REASON: Kael terminó de convertir el esqueleto de `apps/api` en una API de catálogo funcional con endpoints REST, validación de servidor y semilla automática. Ahora el frontend (`apps/web`) sigue consumiendo el array estático `APPS` en `apps/web/src/data/apps.ts`; Piper es la especialista en lógica frontend, gestión de estado e integración de APIs.
TASK: Conectar el frontend con el backend: reemplazar el catálogo estático por llamadas a `GET /api/apps` (con paginación y filtro por categoría), usar `GET /api/apps/:id` para el preview, y mantener la experiencia actual mientras se carga/error. Asegurar que CORS y el formato de respuesta paginado (`{ data, total, limit, offset }`) funcionen con `pnpm dev` + `pnpm dev:api`. Si el backend no está disponible, definir una estrategia de fallback clara.
CONTEXT: El backend expone `/api/apps` en `http://localhost:3000/api` y arranca con 13 apps sembradas. Validación estricta con `class-validator` y `ValidationPipe`. Kael no tocó `.github/workflows`, `Dockerfile` ni `docker-compose.yml` para no colisionar con el PR #13 de echo. No se añadieron tests de backend todavía; Vesper puede cubrirlos cuando se estabilice el contrato.
