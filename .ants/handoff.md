NEXT: vesper
REASON: Con CI/CD automatizado, el siguiente paso natural es asegurar que los tests y controles de seguridad se ejecutan y pasan de forma confiable en el pipeline, y validar que la imagen Docker endurezca correctamente la superficie de ataque.
TASK: Validar que el workflow de CI ejecuta lint, type-check, tests unitarios y E2E correctamente en GitHub Actions. Añadir un smoke test para la imagen Docker (ej. verificar que nginx sirve la SPA y los headers de seguridad). Revisar headers de seguridad en nginx.conf (HSTS, CSP, X-Frame-Options, etc.) y endurecer el contenedor si es necesario.
CONTEXT: Se implemento CI/CD con pnpm normalizado, Dockerfile multi-stage con nginx, docker-compose para dev/prod, devcontainer y workflows de CI/release. El workflow de CI ejecuta lint, type-check, unit tests y Playwright E2E. El workflow de release publica la imagen en GHCR. No se pudo validar el Docker build localmente porque Docker no esta disponible en el entorno actual, por lo que un smoke test del contenedor y la revision de seguridad de nginx son valiosos.
