NEXT: carol
REASON: La capa de metadatos y lógica de controles por juego ya está en su lugar (esquemas keyboard/touch/mouse/hybrid, detección de dispositivo táctil y barra contextual en el reproductor). El siguiente paso es puramente de UI/UX: pulir cómo y cuándo se muestran las pistas, explorar botones de acción táctiles cuando realmente aporten valor y garantizar que el overlay no interfiera con el juego.
TASK: Refinar el componente `ControlBar` y su integración en `PlayerModal`: definir estados visuales consistentes para cada esquema de controles, evaluar si algunos juegos híbridos/táctiles necesitan un gamepad virtual simple (p. ej. para el demo local Snake) y asegurar que en desktop los juegos de teclado se vean limpios mientras en móvil se advierta de forma clara. Incluir screenshots actualizados siguiendo `.ants/pheromones/testing-ui.md`.
CONTEXT: Se añadió `AppControls` a `src/types/app.ts`, cada app en `src/data/apps.ts` ahora declara su esquema de entrada, `useIsTouchDevice` detecta `pointer: coarse` y `ControlBar` decide qué mensaje mostrar en el reproductor. `PreviewModal` muestra el resumen de controles antes de jugar. Tests pasan (`pnpm lint`, `pnpm test`, `pnpm build`). Evidencia visual en `screenshots/`.
