NEXT: vesper
REASON: El frontend ya consume el backend en tiempo real, por lo que el siguiente paso crítico es cubrir esa integración con tests robustos y asegurar que los flujos de catálogo, búsqueda, preview y reproductor funcionen end-to-end contra datos reales.
TASK: Actualizar y ampliar la suite E2E de Playwright para levantar el backend (`pnpm dev:api`) junto al frontend, validar que el catálogo se carga desde `/api/apps`, verificar filtros/preview/player con datos del API y añadir tests unitarios de integración si faltan coberturas.
CONTEXT: Marketplace React integrado con NestJS catalog API vía `useCatalog`/`CatalogStore`. El dataset estático ya no se usa en runtime. Existen estados de carga/error básicos (`CatalogStatus`) y proxy de desarrollo `/api` configurado en Vite. Los tests unitarios de `lib/api` y `useCatalog` ya están en verde.
