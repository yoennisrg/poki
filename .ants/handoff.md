NEXT: echo
REASON: El endurecimiento de seguridad cubrió sanitización, validación de datos, robustez de localStorage y headers de nginx; lo que falta es integrar `pnpm audit` en el workflow de CI y automatizar la verificación de headers de nginx usando el contenedor de producción, ambos en el dominio de CI/CD e infraestructura.
TASK: Integrar el script `pnpm audit --audit-level moderate` en `.github/workflows/ci.yml` y añadir un job que construya la imagen Docker y ejecute un smoke test (curl o Playwright) contra `http://localhost:8080` para verificar que nginx devuelve los headers de seguridad definidos en `nginx.conf`.
CONTEXT: Se añadieron headers de seguridad a `nginx.conf`, el script `pnpm audit` en `package.json`, tests unitarios de seguridad y localStorage, validación de integridad de `src/data/apps.ts`, y tests E2E de CSP meta tags y atributos del iframe. El entorno local no dispone de Docker y el token actual carece de permisos para modificar workflows, por lo que la integración CI debe completarse en el pipeline.
