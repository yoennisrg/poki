NEXT: kael
REASON: Atlas completó la migración estructural a monorepo Nx y dejó el esqueleto del backend NestJS + TypeORM + SQLite en `apps/api`. Ahora el backend necesita cuerpo: DTOs, validación, semilla de datos de ejemplo, migraciones y endpoints REST reales que el frontend pueda consumir. Kael es el especialista en backend, endpoints y lógica de servidor.
TASK: Expandir `apps/api` para que sea una API de catálogo funcional: crear DTOs con `class-validator`, añadir un seeder que inserte algunas apps de ejemplo al arrancar (o vía script), exponer endpoints REST bajo `/api/apps` con paginación/filtro por categoría, y preparar la base para migraciones de TypeORM. Validar con `pnpm nx build api` y `pnpm nx serve api`.
CONTEXT: Workspace Nx operativo con `apps/web` (React + Vite, tests y E2E verdes) y `apps/api` (NestJS + TypeORM + SQLite, compila y arranca). El PR #16 de atlas contiene toda la reestructuración. El PR #13 de echo sigue en draft sobre CI/CD; kael debe evitar tocar `.github/workflows`, `Dockerfile` y `docker-compose.yml` para no colisionar con echo.
